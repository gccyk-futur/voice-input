# Changelog

## 2.0.0 — 2026-08-26

从「语音转录」到「语音工作台」：说过的话能沉淀、能复用。

### 历史与复用
- 新增历史中心（独立窗口）：全部 / 收藏 / 快照三视图，按天分组（今日/昨天 + 日期锚点）、搜索、Markdown/JSON 导出
- 新增收藏：常用记录标星，收藏页「取消全部收藏」仅摘星不删记录
- 新增快照库：常用片段长期保存，支持标题、编辑、删除
- 新增速插面板（二级快捷键）：键盘优先，⇥ 切标签、↑↓ 选择、↵ 插入到目标应用、esc 关闭；底栏显示渠道与版本

### AI 润色
- 新增内置提示词预设库：要点清单、正式公文等模板一键导入，导入拷贝未被修改时随库版本自动更新
- 默认提示词改为中性清理（去口头禅、跟随输入语言输出）；老用户仍在用旧出厂文案时一次性询问是否替换（不静默改写）
- 转录面板底栏显示当前生效的提示词名称（截断 + 悬停全名）

### 界面
- 设置、历史、速插、菜单栏面板全面重新设计（macOS 26 风格：圆角卡片、侧栏材质、统一排版刻度）
- 设置新增使用统计页；文字大小、外观、语言集中在常规页
- 菜单栏版本号、关于页网站链接可点击打开官网；新增使用帮助入口（中英双语说明书）
- 修复空态（无历史/无快照）版式错乱；修复速插面板鼠标划过时列表跟随乱滚

### 配置与数据
- 配置三层防御：每次保存自动备份；主配置损坏自动从备份恢复；主备皆损坏时隔离坏文件并提示，绝不静默重置
- 热重载失败不再静默，会通知用户
- 历史/快照文件损坏时隔离保留现场

### 面向 AI 助手
- 仓库新增 skills/voicekit/SKILL.md：配置引导与排障手册，随代码同仓版本化

### 本地化
- 全部新增文案覆盖 10 种语言；修复繁体中文两处混入简体字

## 1.1.0 — 2026-08-13

新增两个自带密钥（BYOK）云端语音引擎，语音识别正式支持四引擎。

### 语音识别
- 新增讯飞听写引擎：WebSocket 流式识别，支持 wpgs 动态纠错（后文到达时自动修正前文）
- 新增 Deepgram 引擎：WebSocket 低延迟流式识别
- 设置页引擎选择改为四项描述式列表（系统听写 / 阿里云 Fun-ASR / 讯飞 / Deepgram），不再按地区/渠道分组
- 未配置凭据的引擎自动回退系统听写；保存时逐项校验必填字段
- 状态栏「语音引擎」改为下拉菜单，可直接切换四个引擎；切到未配置凭据的引擎时 Toast 提示将回退系统听写
- 设置页云端引擎 API 配置区新增「测试连接」，失败时直接展示服务商返回的原始信息
- 修复讯飞/Deepgram「测试连接」误报失败：讯飞需发送完整最小会话（首帧+数据帧+结束帧）才有响应帧；Deepgram 鉴权在握手阶段完成，空等首帧必超时
- Deepgram 连接失败时透传底层错误原文，不再统一显示「未连接」
- 状态栏连接状态灯覆盖全部云端引擎：阿里云为常驻连接（断开红色警示）；讯飞/Deepgram 为会话制，录音期间显示连接中/已连接，空闲灰色「未连接」
- 引擎启动失败时正确停止音频采集，不再残留录音状态
- 增加开机预缓冲（约 2 秒），修复热键启动瞬间开头几个字被截断的问题
- 快速连按热键的全链路状态处理：取消/空会话静默结束，不再误入错误页

### 界面
- 设置窗口改为侧栏 + 详情双栏布局，修复侧栏被遮挡、最小尺寸异常
- 通用页新增界面语言选择（跟随系统或十种语言之一，重启后生效）
- 状态栏菜单点击外部可靠收起；重复打开 App 时激活已有实例而不是双开
- 设置页重启按钮与应用内单实例保护兼容，重启不再失败

### 配置
- 旧版 config.json 宽容解码：缺字段回退默认值，不再静默重置整份配置

### 本地化
- 补齐全部 10 种语言的历史缺失文案，各语言 key 集合完全一致
- 新增本地化缺失 key 与重复 key 单测，漏翻直接测试失败

### 测试
- 新增讯飞鉴权签名向量单测（官方签名算法对照）
- 新增讯飞 wpgs 结果组装单测（apd/rpl/rg 语义）
- config.example.json 补充 xunfei / deepgram 配置示例

## 1.0.0 (build 28) — 2026-07-18

修复 App Store 审核拒绝（Guideline 2.1(a) / 5.1.1(iv)），并修复官网版沙盒导致的粘贴失效。

### 权限
- 权限页按钮直接触发系统授权弹窗（此前只跳转系统设置，语音识别因此从不出现授权请求、设置列表中也没有本 App）
- 权限状态实时刷新：系统弹窗完成或从系统设置返回 App 后立即更新（此前授权后界面仍显示「未授权」）
- 按审核要求，授权前按钮文案改为「继续」；已被拒绝时显示「打开系统设置」
- 修复点击「继续」后权限回调触发线程隔离断言导致的崩溃（EXC_BAD_INSTRUCTION）
- 官网版辅助功能卡片同样改为触发系统授权提示，App 自动进入辅助功能列表（此前只能手动点「+」添加）

### 官网版
- 移除 App Sandbox（entitlements 拆分为 VoiceMate-Direct.entitlements）：此前官网版与 App Store 版共用沙盒权限清单，导致辅助功能状态假阳性「已授权」、Accessibility 直插与模拟 ⌘V 在真实用户机器上静默失效。App Store 版保持沙盒不变（build-appstore.sh 显式指定）
- 注意：移除沙盒后配置/历史存储路径变更（~/Library/Application Support/VoiceMate），需重新配置一次

## 1.0.0 (build 13) — 2026-07-12

首个正式版本，已上架 App Store。

### 语音识别
- 系统听写：macOS 内置引擎，离线免费，支持中英日韩法德西葡俄意
- 阿里云 Fun-ASR：在线 WebSocket 引擎，高精度，自动标点
- 引擎运行时切换，无需重启
- 静音自动停止（可配置阈值和超时时间）

### AI 润色
- 支持 OpenAI、DeepSeek、Claude、Ollama 及任何 OpenAI 兼容 API
- 流式逐 token 返回，实时显示
- 多模型管理，可自由增删切换
- 自定义系统提示词和用户模板
- 批量连接测试

### 交互
- 全局热键（Carbon + NSEvent 双引擎，沙盒/非沙盒自动适配）
- 悬浮毛玻璃面板（NSPanel + NSVisualEffectView）
- 自动粘贴（Accessibility API 直插 + 剪贴板回退）
- 菜单栏图标自动恢复（定时巡检 + 系统事件监听）

### 设置
- 热键录制控件（键盘捕获，防非法输入）
- 音效自定义（系统内置声音）
- 权限状态一览（麦克风/语音识别/辅助功能）
- 数据流向说明（关于页面）
- 登录时自动启动

### 工程
- 零外部依赖，纯 Apple 框架
- Swift 6 严格并发
- xcodegen 文本化工程定义
- 隐私清单（PrivacyInfo.xcprivacy）
- Universal 架构（arm64 + x86_64）
- DMG 独立分发 + App Store 双渠道
