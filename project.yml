name: VoiceKit
options:
  bundleIdPrefix: me.ckai
  deploymentTarget:
    macOS: "14.0"
  createIntermediateGroups: true
settings:
  base:
    SWIFT_VERSION: "6.0"
    MACOSX_DEPLOYMENT_TARGET: "14.0"
    ARCHS: "arm64 x86_64"
    OTHER_LDFLAGS: "-framework ApplicationServices"
    CODE_SIGN_STYLE: Automatic
    DEVELOPMENT_TEAM: ${VOICEMATE_TEAM_ID}
    PRODUCT_BUNDLE_IDENTIFIER: me.ckai.VoiceMate
    INFOPLIST_FILE: Sources/VoiceKit/Resources/Info.plist
    LD_RUNPATH_SEARCH_PATHS: "$(inherited) @executable_path/../Frameworks"
    ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
    SWIFT_OBJC_BRIDGING_HEADER: Sources/VoiceKit/VoiceKit-Bridging-Header.h
  configs:
    Debug:
      ENABLE_HARDENED_RUNTIME: false
    Release:
      ENABLE_HARDENED_RUNTIME: true
      CODE_SIGN_STYLE: Manual
      CODE_SIGN_IDENTITY: "Developer ID Application: $(VOICEMATE_SIGNING_NAME) ($(VOICEMATE_TEAM_ID))"
      # 官网版默认不带沙盒（Accessibility/事件投递需要）；App Store 构建由
      # build-appstore.sh 通过 CODE_SIGN_ENTITLEMENTS 覆盖为含沙盒的 VoiceMate.entitlements
      CODE_SIGN_ENTITLEMENTS: Sources/VoiceKit/Resources/VoiceMate-Direct.entitlements
      OTHER_CODE_SIGN_FLAGS: "--timestamp --options=runtime"
targets:
  VoiceKit:
    type: application
    platform: macOS
    sources:
      - path: Sources/VoiceKit
    info:
      path: Sources/VoiceKit/Resources/Info.plist
      properties:
        CFBundleName: VoiceKit
        CFBundleDisplayName: VoiceKit
        CFBundleIdentifier: me.ckai.VoiceMate
        CFBundleVersion: "33"
        CFBundleShortVersionString: "1.0.0"
        CFBundlePackageType: APPL
        CFBundleExecutable: VoiceKit
        CFBundleIconFile: AppIcon
        NSPrincipalClass: NSApplication
        LSUIElement: false
        NSMicrophoneUsageDescription: "VoiceKit 需要使用麦克风进行语音输入。"
        NSSpeechRecognitionUsageDescription: "VoiceKit 需要使用语音识别将语音实时转为文字。"
        NSHumanReadableCopyright: "Copyright © 2026 VoiceKit. MIT License."
        LSApplicationCategoryType: public.app-category.utilities
  VoiceKitTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: Tests/VoiceKitTests
      - path: Sources/VoiceKit/ASR/ASRTaskLifecycle.swift
      - path: Sources/VoiceKit/Paste/CmdVEventPlan.swift
      - path: Sources/VoiceKit/Coordinator/RecordingFlowGate.swift
      - path: Sources/VoiceKit/Coordinator/RecordingRecovery.swift
      - path: Sources/VoiceKit/Settings/VoiceKitUIState.swift
      - path: Sources/VoiceKit/ASR/AudioSendDrain.swift
      - path: Sources/VoiceKit/ASR/AudioPreRollBuffer.swift
      - path: Sources/VoiceKit/ASR/AudioPreRollSendGate.swift
      - path: Sources/VoiceKit/ASR/ConnectionEpoch.swift
      - path: Sources/VoiceKit/Paste/TextInsertionPlan.swift
      - path: Sources/VoiceKit/ASR/AudioTapFormatPolicy.swift
      - path: Sources/VoiceKit/Paste/PasteDeliveryPolicy.swift
      - path: Sources/VoiceKit/Config/ConfigImportPolicy.swift
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: me.ckai.VoiceKitTests
        GENERATE_INFOPLIST_FILE: true
